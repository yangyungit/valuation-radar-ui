---
description: 主理人（杨云）个人档案、订阅状态、环境配置（跨会话长期记忆）
alwaysApply: true
---

# 主理人档案（Principal Profile）

> 本文件取代旧的 `USER_CONTEXT.md`，每次新会话自动注入。
> 获得新的稳定信息时由 AI 或主理人手动追加。

---

## 身份
- 姓名：杨云（yangyungit）
- git user.email: `z@yang-yun.com`
- 常用分支：`main`（主链）
- 真相源：**Render 生产环境 + Streamlit Cloud**（99% 时间在线上操作，本地 DB 基本永远是旧的）

## 开发环境（本地 Mac mini，Apple Silicon）
- OS: macOS，Shell: zsh
- Python 虚拟环境：共享 venv 位于 `../system/venv/`
  - 激活：`source ../system/venv/bin/activate`
- 本地后端：`cd valuation-radar && source ../system/venv/bin/activate && python api_server.py` → `http://localhost:8000`
- 本地前端：`cd valuation-radar-ui && source ../system/venv/bin/activate && streamlit run app.py` → `http://localhost:8501`

## 云端部署 & 订阅状态

| 服务 | 套餐 | 关键配额 | URL |
|---|---|---|---|
| **Render**（后端 API） | **Starter ($7/月)** ⭐ | 0.5 vCPU / 512 MB RAM / **1 GB 持久盘（已挂 `/opt/render/project/src/data`）/ 永不休眠** / 100 GB 带宽 | `https://valuation-radar-server.onrender.com` |
| Render Cron Job | 独立按分钟计费 | Starter 档 $0.00016/分钟，最低 $1/月起；z_scanner 单次约 $0.003 | （2026-04-19 状态：待确认是否已配置） |
| **Streamlit Cloud**（前端） | Free | ~2.6 GB RAM / 共享 CPU | `https://yangyun-macro.streamlit.app/` |

## Render 持久盘内容（data/，**丢失即系统瘫痪**）
- `narrative.db`：新闻语料、GDELT slot 去重表（`gkg_fetched_slots`）、TF-IDF 词典、daily_cluster_stats、pending_inbox、noise_words、quality_log
- `universe.db`：A/B/C/D/Z 四组候选标的、`UniversalClassifier` 迟滞状态、资格状态变更记录

## 核心业务红线（详版见 `architecture-constraints.mdc`）
- `my_stock_pool.py` 中 A/B/C/D 四大战术分组是**最高业务机密**，绝不允许修改名称/合并/增删
- 前端（`valuation-radar-ui`）禁止存放业务逻辑和因子字典，必须通过 `api_client.py` 调后端
- UI 渲染必须用显式 `for` 循环（不用列表推导式生成 Streamlit 组件）

## 协作偏好
- **语言**：必须用中文交流（代码/变量名保持英文）
- **编辑风格**：精准行级编辑优先，debug 探针用完立即移除
- **沟通**：直接、简洁，不废话；重要决策先解释再动手
- **报错处理**：遇错不要无限循环自调试，告知主理人由人决策下一步
- **Git**：push 到 main 已授权自动执行，严禁 `--force` / `amend` / `rebase` / `git add -A` / 改 config（详见 `core-protocols.mdc` Git 章节）

## 本地自动化
- **crontab（本地 Mac mini）**：每周日 03:00 自动运行 `z_scanner.py`，日志在 `valuation-radar/z_scanner.log`

## 已安装依赖（系统级备忘）
- `finviz==2.0.0`（已装到 `system/venv`，2026-04-12）
- `curl_cffi`（Yahoo Crumb v2 反爬绕过，2026-04-18）

## 长期待办
- [ ] Render 上配置 Cron Job 运行 `z_scanner.py`（每周日），实现云端 Z 级雷达全自动化
- [ ] SEC `WATCHED_ISSUERS` 定期补充新发行人
- [ ] 评估 STRD（Strategy 10% 非累积优先股）和 STRC（浮动利率月付优先股）加入 `Z_SEED_POOL`

---

## 变更记录
- **2026-04-19**：从 `USER_CONTEXT.md` 迁移至 `.cursor/rules/principal-profile.mdc`，顺手订正三处错误
  - Render Starter 是**永不休眠**（原文误写"自动休眠，冷启动 30-60s"——那是 Free 层）
  - 后端 URL 补 `-server` 后缀（`valuation-radar-server.onrender.com`，与代码 `api_client.py:24` 一致）
  - 补持久盘内容清单（`narrative.db` + `universe.db` 明细）
