---
description: 前端核心协作协议——数据一致性指针 / 语言规范 / 真相源 / Git 工作流 / 本地环境 / 开发日志
alwaysApply: true
---

# 前端核心协作协议（Core Protocols — Frontend）

本文件汇总所有**每次会话都适用**的协作协议，从 `.cursorrules` 迁移而来。与后端 `valuation-radar/.cursor/rules/core-protocols.mdc` 联合生效。

---

## 1. 📐 数据一致性协议指针 ⭐ 最高优先级

**数据一致性协议与核心字段数据流图以后端仓库为正本，前端不保存副本。**

任何改动涉及 `push_*` / `_save_*` / `_record_*` / `_sync_*` 函数、跨页面 `session_state` 共享、本地 JSON 降级兜底的逻辑时，**动手前必须先读**：

- 数据流图：`../valuation-radar/DATA_FLOW.md`
- 硬约束：`../valuation-radar/DATA_CONSISTENCY_PROTOCOL.md`
- 后端 `valuation-radar/.cursor/rules/data-consistency.mdc` 为摘要版

跨页面共享的 `session_state` key 必须使用 `shared_state.py` 的 `SharedKeys` 常量，**禁止字符串字面量散落**。

---

## 2. 🗣️ 交流语言规范

**必须使用中文**与主理人交流，包括所有回复、解释、问题和建议。代码注释、变量名、API 字段等技术内容保持英文不变。

---

## 3. 🌐 真相源判别协议（Source-of-Truth Protocol）⭐ 最高优先级

项目是**双人协作**，两人共用同一 GitHub 仓库，但**日常真相源完全不同**，不要混为一谈：

| 角色 | git user.email | 常用分支 | 真相源 | 调试习惯 |
|---|---|---|---|---|
| **主理人（杨云）** | `z@yang-yun.com`（或 user.name = `yangyungit`） | `main` / 主链 | **Render 生产环境** ⭐ | 99% 时间在线上操作 |
| **同事（傲阳）** | 非上述身份 | 支链 / feature 分支 | **本地 localhost** | 大部分时间本地调试 |

**每次涉及运行时数据（`universe.db` / `arena_history` / `conviction_state` / `screen_results` 等）排障前，必须先跑一次识别命令：**

```bash
git config user.email && git branch --show-current
```

### 主理人场景（email = z@yang-yun.com 或 main 分支）

- **Render 后端地址**：`https://valuation-radar-server.onrender.com`
- **真相源判定顺序（严格遵守）**：
  1. **优先**：`curl https://valuation-radar-server.onrender.com/api/v1/xxx` 直接查 Render API
  2. **其次**：问主理人前端页面上看到的内容、成功/失败 toast 的原话
  3. **最后**：才去翻本地 `universe.db` 的 mtime、本地进程 `ps aux | grep`、本地 JSON 兜底文件
- **禁止的错误推理链**：不要用本地 `universe.db` 的 mtime 反推"主理人今天有没有操作过系统"——他大概率在 Render 上操作，本地文件**永远是旧的**，这个时间戳对线上行为毫无证据价值。
- **本地进程离线 ≠ 系统没跑**：`lsof -iTCP:8000` 没监听、`ps | grep api_server` 无进程，只说明主理人没启动本地副本，**不能推出"Render 后端也没跑"**。Render 付费档（Starter 及以上，含当前的 Standard）是**永不休眠**的托管服务。

### 同事场景（傲阳，非主理人 email 或在支链分支）

- **真相源直接在本地**：`universe.db` / `data/arena_history.json` / 本地进程 8000/8501 端口
- 不要盲目让他 `curl Render`，他的修改很可能还没合并到主链，Render 上根本不会有
- 调试指令配合本地：`lsof -iTCP:8000`、`ps aux | grep api_server`、本地 SQLite 路径都是有效证据

### 拿不准时的默认行为

如果 `git config user.email` 返回**不是** `z@yang-yun.com` 也不是明显的傲阳标识，或者当前分支不在 `main` / 支链已知命名约定里，**先问一句**"你是主理人还是傲阳？当前在调试 Render 还是本地？"再动手，不要猜。

---

## 4. 🖥️ 本地开发环境（Local Dev Environment）

- **操作系统兼容性**：团队采用跨平台协作。当你（AI）需要执行 Shell 命令时，请先侦测当前运行的操作系统（macOS/Linux 使用 bash/zsh，Windows 使用 PowerShell/cmd）并使用对应的兼容命令。
- **Python 虚拟环境**：项目约定在各项目目录的**上级目录**下存放共享虚拟环境（如 `../system/venv/`）。激活命令因系统而异：macOS/Linux 用 `source ../system/venv/bin/activate`，Windows 用 `..\system\venv\Scripts\activate`。**请勿硬编码任何绝对路径。**
- **本地调试启动**：请运行本项目根目录下的启动脚本 `start_dev.sh`（macOS/Linux）或 `start_dev.bat`（Windows），脚本会自动检测虚拟环境并依次启动后端与前端。
- **前端访问地址**：http://localhost:8501
- **API 自动切换**：`api_client.py` 已内置操作系统判断，本地开发自动对接 `localhost:8000`，无需手动改代码。
- **给出 Shell 命令时**：必须包含虚拟环境激活步骤，禁止假设 streamlit/uvicorn 已在 PATH 中。根据操作系统使用对应的激活命令，**禁止硬编码绝对路径**。

---

## 5. 📋 开发日志规范（Dev Log）

- **及时记录变动**：每当完成一项**复杂变动**（新功能、架构调整、重要 Bug 修复、API 接口变更、数据结构重构等），必须在 `DEV_LOG.md` 中追加一条记录。
- **记录格式**：以 `## YYYY-MM-DD` 为日期标题（同一天多条可合并），简述变动内容、原因和影响范围，保持中文叙述。
- **触发标准**：凡涉及跨文件改动、新增页面/模块、修改数据流或 API 契约的变动，均视为复杂变动，必须记录；单文件小幅样式调整可免。

---

## 6. 🚀 Git 工作流：自动提交并推送（Auto Git Commit/Push）⭐ 主理人授权

**默认行为**：完成一项实质性改动后，**直接**执行 `git add <本次改动的具体文件路径> && git commit && git push` 到**当前分支**（通常是 `main`），**无需再问主理人**。主理人已明确授权——真相源是 GitHub + Streamlit Cloud / Render 自动部署链路，不 push 等于改动没生效。

**严禁 `git add -A` / `git add .`**：工作区常有未跟踪的本地私有文件（`.python-version`、`macro-radar.code-workspace`、`带鱼.md`、`data/_test_price_cache.pkl` 等）和运行时数据库（`narrative.db` / `universe.db`）；盲加会误提交。必须用**显式文件路径** `git add`，且 `git add` 前先跑一次 `git status` 核对范围。

### 触发标准（满足任一即可提交）

1. 至少修改 1 个受跟踪文件且 `ReadLints` 无新增错误；
2. 已在 `DEV_LOG.md` 追加对应条目（若属"复杂变动"）；
3. 主理人说"推一下 / 提交吧 / 改完了 / 部署上去"。

### Commit message 规范

- **中文**，动词开头（修复 / 新增 / 重构 / 优化 / 删除 / 补充 / 文档 等）；
- 首行 ≤ 60 个汉字，概括"改了什么 + 为什么"，不堆砌形容词；
- 正文（可选）补证据链，如"根因：…；方案：…"；
- 使用 heredoc 格式：`git commit -m "$(cat <<'EOF' ... EOF)"`；
- **不签名，不加任何 AI/Cursor 标识/co-author**（保持 git log 整洁）。

### 红线（以下情况必须先停下来问主理人，绝不自作主张）

1. 强制推送：`git push --force` / `git push --force-with-lease`；
2. 修改 `git config` / 远端 URL / 分支上游；
3. 提交涉及 **`.env` / `credentials*` / `secrets*` / `narrative.db` / `universe.db` / `_test_price_cache.pkl`** 等敏感或业务运行时数据的文件——即使 `git status` 已列出也**一律不加入 stage**；
4. `rebase` / `commit --amend`（主理人未明确要求时）；
5. 新建 / 切换 / 删除分支；
6. 单次提交删除文件 > 5 个，或净行数变动 > 500 行——先汇总给主理人确认影响面；
7. 跨仓库改动（同时改 `valuation-radar-ui` 和 `valuation-radar`）时，**必须两仓分别独立 commit + push**，且先推后端再推前端（避免前端引用后端尚未部署的新契约）。

### 最小纪律

- **无改动不提交**：`git status` 无变化时不产生空 commit；
- **推送前跑 lint**：有 Python / TS 改动必先 `ReadLints`，有新增错误必须修掉再推；
- **不主动拉远端**：除非主理人明确要求 `git pull`，不要单方面 merge / rebase 远端；
- **推送失败不重试 force**：`git push` 被 rejected（非快进），立刻停下来汇报，严禁用 `--force` 强推覆盖主理人或同事的改动。
