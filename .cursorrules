# Moltbot UI - 前端开发环境规范

本文件是 `valuation-radar-ui` 前端项目的专属规则，与后端 `valuation-radar/.cursorrules` 联合生效。

## 🗣️ 交流语言规范
* **必须使用中文**与主理人交流，包括所有回复、解释、问题和建议。代码注释、变量名、API 字段等技术内容保持英文不变。

## 🖥️ 本地开发环境 (Local Dev Environment)
* **操作系统兼容性：** 团队采用跨平台协作。当你（AI）需要执行 Shell 命令时，请先侦测当前运行的操作系统（macOS/Linux 使用 bash/zsh，Windows 使用 PowerShell/cmd）并使用对应的兼容命令。
* **Python 虚拟环境：** 项目约定在各项目目录的**上级目录**下存放共享虚拟环境（如 `../system/venv/`）。激活命令因系统而异：macOS/Linux 用 `source ../system/venv/bin/activate`，Windows 用 `..\system\venv\Scripts\activate`。**请勿硬编码任何绝对路径。**
* **本地调试启动：** 请运行本项目根目录下的启动脚本 `start_dev.sh`（macOS/Linux）或 `start_dev.bat`（Windows），脚本会自动检测虚拟环境并依次启动后端与前端。
* **前端访问地址：** http://localhost:8501
* **API 自动切换：** `api_client.py` 已内置操作系统判断，本地开发自动对接 `localhost:8000`，无需手动改代码。
* **给出 Shell 命令时：** 必须包含虚拟环境激活步骤，禁止假设 streamlit/uvicorn 已在 PATH 中。根据操作系统使用对应的激活命令，**禁止硬编码绝对路径**。

## 📋 开发日志规范 (Dev Log)
* **及时记录变动：** 每当完成一项**复杂变动**（新功能、架构调整、重要 Bug 修复、API 接口变更、数据结构重构等），必须在 `DEV_LOG.md` 中追加一条记录。
* **记录格式：** 以 `## YYYY-MM-DD` 为日期标题（同一天多条可合并），简述变动内容、原因和影响范围，保持中文叙述。
* **触发标准：** 凡涉及跨文件改动、新增页面/模块、修改数据流或 API 契约的变动，均视为复杂变动，必须记录；单文件小幅样式调整可免。

## 🏗️ 前端架构约束
* **物理隔离：** 前端页面 (`pages/`) 严禁存放业务逻辑和因子字典，所有计算必须通过 `api_client.py` 向后端请求。
* **UI 渲染安全：** 严禁列表推导式直接生成 Streamlit 组件，必须用显式 `for` 循环。
* **全局缓存控制：** 每个页面侧边栏必须保留 `st.cache_data.clear()` 一键强刷按钮。
* **绘图安全：** 数据送入 Plotly 前必须 `.astype(float)` + `.dropna()`。

## ✂️ UI 文案简洁原则 (Concise Copy Protocol)
* **图表标题：** 只写核心信息，禁止堆砌修饰词。例：用「累计收益率」而非「持仓段首尾相接累计收益率走势对比图」。
* **坐标轴 / 图例标签：** 能用符号或缩写解决的绝不写完整句子；日期用 `YYYY-MM` 格式，不加「年」「月」等汉字。
* **旁白 / 说明卡片：** 每张卡片不超过 3 句话，每句不超过 25 字；能用数字量化的结论不用文字描述。
* **按钮 / 选项文案：** 2～6 个汉字为宜，禁止在按钮里写完整句子。
* **Tooltip / Hover 文字：** 只显示数值 + 单位，不附加解释性长句。
* **自我审查：** 每次生成包含中文 UI 文案的代码前，先在心里过一遍：「这句话能不能砍掉一半？」——如果能，就砍。

## 🔤 UI 字号规范 (Font Size Standard)
所有自定义 HTML/CSS 块（旁白卡片、白盒展开区、formula-box、conclusion-box 等）必须遵守以下最小字号约束：
* **正文 / 描述文字：** 最小 **13px**（禁止使用 11px、12px）。
* **标题 / 区块 header：** 最小 **15px**。
* **脚注 / 低优先级标注：** 最小 **13px**（原来常见的 `font-size:11px` 全面禁用）。
* **CSS 类 `.ev-item`（证据列表）：** 14px。
* **CSS 类 `.formula-box` / `.conclusion-box`：** 14px。
* **CSS 类 `.sub-ticker`（龙头穿透行）：** 13px。
* 新增任何 inline `style='font-size:...'` 时，必须先对照上述规范检查，不得低于对应层级最小值。
